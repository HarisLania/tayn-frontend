/**
 * @deprecated Superseded by CatalogService (categories + plans + metadata).
 * Kept as a thin alias for backward compatibility.
 */
export { CatalogService } from './catalog.service';
export { CatalogService as PlanService } from './catalog.service';
